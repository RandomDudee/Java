public class PrimativeAndReferenceTypes {

    public static void main(String[] args) {
        int myNumber = 5; // Variable myNumber stored the actual number 5

        String message = "Hello"; // Variable message doesn't store the string "Hello" instead it is held in memory. the variable message stores the location of string message.

        message = "World";

        System.out.println(message);
    }

}

